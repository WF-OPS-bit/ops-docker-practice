# Redis 主节点配置
#
# 注意：requirepass / masterauth 故意不写在这里。
# Redis 不会展开 ${REDIS_PASSWORD} 这类变量，写进去会被当成字面量密码。
# 密码统一由 docker-compose.yml 的 command 以启动参数传入，
# 这样配置文件保持干净（无密钥），可以直接提交到仓库。

bind 0.0.0.0
port 6379

appendonly yes
dir /data
