# Redis 从节点配置
#
# 与主节点的差异只有一行 replicaof。
# requirepass / masterauth 同样由 compose 的 command 启动参数传入，不写在这里。

bind 0.0.0.0
port 6379

# 指向主节点。容器名 redis-master 由 docker 内置 DNS 解析，
# 不需要固定 IP，容器重建后 IP 变了也不受影响
replicaof redis-master 6379

appendonly yes
dir /data
