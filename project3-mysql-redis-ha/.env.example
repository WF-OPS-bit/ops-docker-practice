# 复制成 .env 后再改密码；.env 本身不提交
#
# 同样是"只用字母数字"：这些值会被 compose 替换进 YAML 与命令行，
# $ " \ & # 空格 这类字符会把配置解析坏

MYSQL_ROOT_PASSWORD=ChangeMe_Root_123

# 主从复制专用账号的密码（由 scripts/init-replication.sh 创建）
REPL_PASSWORD=ChangeMe_Repl_123

REDIS_PASSWORD=ChangeMe_Redis_123
